services:
  # Claude Code with all plugins — runs REAL prompts
  live-test:
    build:
      context: .
      dockerfile: Dockerfile.live
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      # Route directly to user's proxy
      - ANTHROPIC_BASE_URL=http://host.docker.internal:9212
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - live-workspace:/workspace
      # Mount all plugins
      - ../../claude-autofix:/home/testuser/.claude/plugins/autofix
      - ../../claude-guardian:/home/testuser/.claude/plugins/guardian
      - ../../claude-workflows:/home/testuser/.claude/plugins/workflows
      - ../../claude-worktrees:/home/testuser/.claude/plugins/worktrees
      - ../../claude-autoresearch:/home/testuser/.claude/plugins/autoresearch

volumes:
  live-workspace:
