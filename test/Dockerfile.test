FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends git && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Copy test files
COPY test_suite.py .
COPY hook_simulator.py .
COPY mcp_client.py .

CMD ["python", "test_suite.py"]
